[extend]
useDefault = true

# No broad path allowlists -- use .gitleaksignore with specific fingerprints instead.
# If a test fixture or preset legitimately contains a secret-like string,
# suppress the specific finding by fingerprint in .gitleaksignore.
#
# See: https://github.com/gitleaks/gitleaks?tab=readme-ov-file#configuration
