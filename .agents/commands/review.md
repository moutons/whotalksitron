Review the current work against the active spec and plan:

1. Identify the current spec from `docs/superpowers/specs/` and plan from `docs/superpowers/plans/`.
2. Check which plan tasks are completed (look for commits since the plan was written).
3. For completed tasks, verify:
   - Code matches what the plan specified
   - Tests exist and pass (`just test`)
   - No unintended changes outside the task scope
4. For the next incomplete task, confirm readiness:
   - Prerequisites are met
   - Files referenced in the task exist
5. Report: tasks done, tasks remaining, any discrepancies between plan and implementation.
